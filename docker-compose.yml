services:
  web:
    build: .
    networks:
      - internal
    volumes:
      - ./data:/app/data
    restart: always

  nginx:
    build: ./nginx
    ports:
      - "3000:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/nginx_certs:/etc/nginx/ssl
    depends_on:
      - web
    networks:
        - internal
        - external
    restart: always


networks:
    internal:
      internal: true
    external:
      driver: bridge